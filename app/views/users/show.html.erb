<div class="row mb-3 mt-3">
  <div class="col-lg-12">
    <% if user_signed_in? %>
      <p class="float-right ml-auto w-auto">
        <%= link_to "Редактировать свой профиль", edit_user_path(@user), class: 'btn btn-outline-info mt-3' %>
      </p>
    <% end %>
    <div class="field mb-3">
      <span class="text-muted">
        Имя:
      </span>
      <span class="text-dark">
        <%= @user.name %>
      </span>
    </div>
    <div class="field">
      <span class="text-muted">
        Почта:
      </span>
      <span class="text-dark">
        <%= @user.email %>
      </span>
    </div>
    <div >
      <%= image_tag user_avatar(@user), class: 'avatar-profile' %>
    </div>
  </div>
</div>

<h4 class="mb-3">Cобытия пользователя<%= @user.name %></h4>
  <div class="row">
    <% if @user.events.any? %>
    <%= render partial: 'partials/event', collection: @user.events %>
    <% else %>
    Пока нет ни одного события.
    <% end %>
  </div>


